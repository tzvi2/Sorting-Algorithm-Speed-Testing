Sort algorithm testing site.

Allows input of 1-d arrays. Choose which sort to use. How many times do you want it to perform the sort. And list the average time it took to perform all of those sorts.
Also include option to generate a random array of length _
on each sorting algos page include a summary of how it works
